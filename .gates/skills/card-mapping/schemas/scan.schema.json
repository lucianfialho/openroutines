{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "card-mapping varredura output (LLM portion)",
  "type": "object",
  "required": ["profile", "coreSectionsComplete", "goldenRoutes"],
  "properties": {
    "profile": {
      "type": "object",
      "description": "Section title -> filled markdown content, one entry per template section.",
      "additionalProperties": { "type": "string" }
    },
    "coreSectionsComplete": {
      "type": "boolean",
      "description": "true when all 8 auditable-core sections were filled with real content."
    },
    "goldenRoutes": {
      "type": "array",
      "description": "Critical product routes detected by the framework heuristic (empty for a repo with no front-end/HTTP routes).",
      "items": { "type": "string" }
    }
  }
}
