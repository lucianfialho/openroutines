#!/usr/bin/env bash
# scripts/supply-chain/npm (F4 #156) -- supply-chain guard shim for npm.
# See _guard.sh in this directory for the shared logic.
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_guard.sh"
run_guarded_shim "npm" "$@"
